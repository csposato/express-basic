const { TestScheduler } = require("jest")

const app = require('express')
const userRoute = require('userRoute')
const { request } = require("express")

describe('Test get users path', function() {
    request
})